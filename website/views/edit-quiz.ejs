<!DOCTYPE html>
<html lang="en">
  <head>
    <meta vharset="" UTF-8 />
    <title>Create Quiz</title>
    <link rel="stylesheet" href="/styles/edit-quiz.css" />
    <link rel="icon" type="image/png" href="/assets/OhmcordLogo.png" />
  </head>
  <body>
    <div id="page-container">
      <div class="header">
        <a href="/"><img class="logo" src="/assets/OhmcordLogo.png" /></a>
        <h1>Erstelle ein Quiz</h1>
        <h3 id="hash-url" data-quiz-id="<%= locals.id %>"><%= locals.hash %></h3>
      </div>

      <div id="quiz-container">
        <div id="settings-container">
          <h3>Einstellungen:</h3>
          <form id="quiz-form" onsubmit="return false;">
            <label for="quiz-title"><h4>Quiz Titel:</h4></label>
            <input id="quiz-title" type="text" placeholder="Enter quiz title" name="quiz_title" value="<%= locals.title %>" />

            <label><h4>Sichtbarkeit der Ergebnisse:</h4></label>
            <input type="radio" id="show-everyone" name="visibility" value="2" <%= locals.visibility === 2 ? "checked" : "" %> />
            <label for="show-everyone">Alle Teilnehmer sichtbar</label><br />

            <input type="radio" id="top-three" name="visibility" value="1" <%= locals.visibility === 1 ? "checked" : "" %> />
            <label for="top-three">Nur Plätze 1 - 3 sichtbar</label><br />

            <input type="radio" id="hide-all" name="visibility" value="0" <%= locals.visibility === 0 ? "checked" : "" %> />
            <label for="hide-all">Niemand ist sichtbar</label>
          </form>
        </div>

        <div id="questions-container">
          <div id="question-heading-container">
            <h3 id="questions-heading" data-question-id="null">Neue Frage:</h3>
            <button id="delete-question-button" onclick="removeQuestion()">&#10006;</button>
          </div>

          <div id="create-question-container">
            <form id="question-form">
              <input
                type="text"
                id="question-input"
                placeholder="Frage..."
                onkeypress="newQuestionEnter(event)"
                onchange="newQuestionInput(event); return false;"
              />
              <button id="add-answer-button" onclick="addAnswer(); return false;">+</button>
            </form>
          </div>
          <div id="question-menu">
            <button id="previous-question-button" onclick="showPreviousQuestion()">Vorherige Frage</button>
            <button id="finish-button" onclick="saveAndFinish()">Speichern und Beenden</button>
            <button id="next-question-button" onclick="nextQuestion()" disabled>Nächste Frage</button>
          </div>
        </div>
      </div>

      <div class="footer"></div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="/scripts/edit-quiz.js"></script>
  </body>
</html>
